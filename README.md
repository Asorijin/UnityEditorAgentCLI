# UnityEditorAgentCLI
Unity Editor plugin that brings Claude Code CLI into a native chat window — streaming responses, context-aware conversations, and bubble-style message UI right inside the Unity Editor.
